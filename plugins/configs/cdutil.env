# Array of folders to crawl
CDUTIL_BASEFOLDER_LIST=( 
    "$HOME/docker-repos"
    "$HOME/zcmd-quickstart"
)

# Array of additional folders to crawl (set this in your custom-profile.env)
CDUTIL_CUSTOM_LIST=()

